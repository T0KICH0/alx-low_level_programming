#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - entry point
 *
 * Return: 0 after printing the function
 */
int main(void)
{
int ch = 0;
for (ch = 0; ch < 10; ch++)
{
printf("%d", ch);
}
printf("\n");
return (0);
}
